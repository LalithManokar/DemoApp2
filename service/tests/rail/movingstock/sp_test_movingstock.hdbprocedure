PROCEDURE "SAP_TM_TRP"."sap.tm.trp.service.tests.rail.movingstock::sp_test_movingstock" (
   OUT CNT INTEGER ) 
	LANGUAGE SQLSCRIPT
	SQL SECURITY INVOKER 
	--DEFAULT SCHEMA <default_schema_name>
	READS SQL DATA AS
BEGIN
/***************************** 
	Write your procedure logic 
 *****************************/
 
 
     SELECT count(*) into CNT
       FROM
					"_SYS_BIC"."sap.tm.trp.db.movingstock/cv_freight_booking_moving_stock"(
					placeholder."$$LOCATION_FILTER_ID$$"=>'2003',
					placeholder."$$EQUIP_FILTER_ID$$"=>'152',
					placeholder."$$START_DATE_TIME$$"=>'',
					placeholder."$$END_DATE_TIME$$"=>'',
					placeholder."$$TIME_FILTER_BY$$"=>'NO_FILTER',
					placeholder."$$LOCATION$$"=>'',
					placeholder."$$EQUIPMENT_TYPE$$"=>''
					);
 
 
 
END;
