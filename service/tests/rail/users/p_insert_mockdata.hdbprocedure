PROCEDURE "SAP_TM_TRP"."sap.tm.trp.service.tests.rail.users::p_insert_mockdata" (
    OUT USERID INTEGER
)
   LANGUAGE SQLSCRIPT
   SQL SECURITY INVOKER
   DEFAULT SCHEMA SAP_TM_TRP
AS
BEGIN
   select ID into USERID from "sap.tm.trp.db.dataprotectionprivacy::v_user_for_join" where USERNAME='SYSTEM';
   insert into "sap.tm.trp.db.systemmanagement.user::t_role" values(99998,5,'UT_RES1','UT_DESC',2,'',3,'','');
   insert into "sap.tm.trp.db.systemmanagement.user::t_user_role" values(:USERID,99998);
END