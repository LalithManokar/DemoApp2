schema = "SAP_TM_TRP";
query  = "SELECT 1 AS ID,
                 'EURO 1 MODEL' AS NAME,
                 'EURO 1 MODEL' AS DESC
            FROM DUMMY
           UNION
          SELECT 2 AS ID,
                 'US DOLLAR 1 MODEL' AS NAME,
                 'US DOLLAR 1 MODEL' AS DESC
            FROM DUMMY";
