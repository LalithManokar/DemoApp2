schema = "SAP_TM_TRP"; 
query = "SELECT PROPERTY, VALUE
         FROM M_PASSWORD_POLICY
         WHERE PROPERTY = 'last_used_passwords'
         OR    PROPERTY = 'minimal_password_length'
         OR    PROPERTY = 'password_layout'";