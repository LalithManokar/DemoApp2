schema = "SAP_TM_ROUTING";
query = "SELECT id, 
        id AS external_id, 
        id AS trip_name, 
        path_id,
        rule_number, 
        from_location, 
        to_location, 
        departure_time, 
        arrival_time
        FROM \"sap.tm.trp.routing.db.dataset::t_trip\" AS t
";
    
depends_on_table = ["sap.tm.trp.routing.db.dataset::t_trip"];