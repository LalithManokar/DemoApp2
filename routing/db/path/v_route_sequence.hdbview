schema = "SAP_TM_ROUTING";
query = "select route_id, 
    sequence,
    location,
    pre_trip_sequence,
    next_trip_sequence,
    basic_path_id,
    path_type,
	mtr,
	trip_id,
	trip_name,
	departure_time,
	arrival_time,
	distance,
	storage_cost,
	handling_cost,
	transport_cost,
	cost_currency,
	handling_type
    FROM \"sap.tm.trp.routing.db.path::t_route_sequence\"
";
depends_on_table = ["sap.tm.trp.routing.db.path::t_route_sequence"];