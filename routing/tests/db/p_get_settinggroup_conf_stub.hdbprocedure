PROCEDURE "SAP_TM_ROUTING_TEST"."sap.tm.trp.routing.tests.db::p_get_settinggroup_conf_stub" (
    IN setting_group_id BIGINT,
    IN usage VARCHAR(50),
    OUT OUTPUT TABLE (NAME NVARCHAR(50), VALUE NVARCHAR(50))
)
   LANGUAGE SQLSCRIPT
   SQL SECURITY INVOKER
   DEFAULT SCHEMA SAP_TM_ROUTING_TEST
   READS SQL DATA AS
BEGIN
   /*************************************
       Write your procedure logic 
   *************************************/
   output = 
   SELECT name, value
   FROM "sap.tm.trp.routing.tests.db::t_setting_group_conf_test"
   WHERE setting_group_id = :setting_group_id AND usage = :usage;
END